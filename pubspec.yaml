name: octopus_vpn_core
description: "Client VPN indépendant Flutter V2Ray"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Forcer une version stable compatible V1/V2
  flutter_v2ray: 0.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/config.json
    
